#服务相关
server:
  port: 8081
  
spring:
  #数据源
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.41.129/school
    username: root
    password: root
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
  profiles:
    #开发模式
    active: dev
    #热部署生效
  devtools:
    restart:
      enabled: true